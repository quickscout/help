---
title: "Search"
description: "Find wells, leases, operators, fields and places by name or number."
---

Search is free with an account. Click the search box at the top of the map, or press <kbd>⌘</kbd> <kbd>K</kbd> (<kbd>Ctrl</kbd> <kbd>K</kbd> on Windows).

{/* screenshot: maps-search — suggestions for Pioneer */}

## Search by name

Type at least three letters. Suggestions show leases, fields, operators and wells with their current production. Click one to open it.

Press <kbd>Enter</kbd> to see every match in a results list. It has tabs for **ALL**, **LEASES** and **WELLS**. In the list, <kbd>Tab</kbd> moves, <kbd>Enter</kbd> opens, and <kbd>Space</kbd> selects.

## Search by number

Type a number and QuickScout offers to read it as an **API/UWI**, **Operator**, **Field** or **Lease** number:

- An **API** opens that well.
- An **operator**, **field** or **lease** number puts every matching well on the map.

## Search terms

The results list understands these shortcuts:

| Type | Finds |
|---|---|
| `operator:pioneer` | Records for that operator |
| `county:midland` | Records in that county |
| `field:spraberry` | Records in that field |
| `district:08` | Records in an RRC district |
| `oil>1000` | Leases making at least 1,000 bbl/d of oil (for wells: cumulative bbl). `<` works too. |
| `gas>5000` | Leases making at least 5,000 mcf/d of gas (for wells: cumulative mcf) |
| `wells>10` | Leases with at least 10 wells |

With the box empty, **List everything in view** lists every record on screen.

## Search pills

Each search you run leaves a pill in the top bar. Click the pill to zoom to its results, or its **×** to clear it from the map.

## Advanced Search

For record lookups and places, press <kbd>⌘</kbd> <kbd>⇧</kbd> <kbd>K</kbd>, click the sliders button in the search box, or click the QuickScout logo. From there you can:

- **Go to a place**: a county, abstract, RRC district, or latitude/longitude.
- **Go to an RRC record**: an API, lease, field or operator number.
- **Save this destination** (the bookmark button) to reuse it later.
- See **what's on the map**, zoom to it, or hide other wells.

## Recents

With the search box empty, you'll see **Recent Searches** and **Recent Items**. The **Recents** button on the left rail has the full list.
