---
title: "Data tables"
sidebarTitle: "Data tables"
description: "Browse every well, lease, operator, field, P-4, frac job, satellite pad and rig site in Texas as a table."
---

The data tables sit beside the map. You need to be signed in to use them.

- Click the **Tables** handle on the map's right edge to open them. Drag the handle to resize.
- **Fill the window**, at the top right, gives the tables the whole screen. **Show the map again** or <kbd>Esc</kbd> brings the map back.
- The **×** (**Hide the tables**) hides them.

<Frame>
  <img src="/images/data-panel.webp" alt="The Wells table open beside the map, listing the wells in view by cumulative oil" />
</Frame>

<Tip>
  When the tables are narrow, the buttons show icons only and the tab counts are hidden. Widen the tables, or click **Fill the window**, to see the labels used on this page.
</Tip>

## Tabs

| Tab | One row is |
|---|---|
| **Wells** | A well. Historic wells can share an API number, so an API may appear more than once. |
| **Leases** | A lease (district + lease number). For gas, one row is one well. |
| **Operators** | An operator, totalled over its leases in view. See [Operators & Fields](/maps/operators-fields#operators). |
| **Fields** | An RRC field, with its lifetime totals and how much the map can place. See [Operators & Fields](/maps/operators-fields#fields). |
| **P-4** | An operator transfer. Switch **Rows** to see one row per **Transfers** or per **Filings**. |
| **Frac** | A FracFocus treatment. |
| **Pads** | New ground detected from satellite imagery. |
| **Rigs** | A site watched by radar for rigs and frac spreads. |
| **Deals** | Part of Enterprise, which is invite-only for now. |

When the tables are wide enough, every tab except Pads and Rigs shows its statewide row count. A dot on a tab means it has filters on.

### Choose your tabs

**Wells**, **Leases**, **Operators**, **Fields**, **P-4** and **Deals** are shown at first. To add **Frac**, **Pads** or **Rigs**, click **Add tab** at the end of the tab row and pick one under **Add a tab**.

The same popover arranges your tabs: under **On the strip · drag to reorder**, drag a tab (or press <kbd>Alt</kbd> + <kbd>↑</kbd> / <kbd>↓</kbd>) to move it, and click its **×** to remove it. Your tabs are remembered for your account.

Each tab keeps its place when you switch away and come back: its scroll position and ticked rows, as long as its rows haven't changed. Its search, filters and sort are kept too.

## The top row

<Frame>
  <img src="/images/data-toolbar.webp" alt="The tables filling the window: tabs with their counts, the search box, and the toolbar with its labels" />
</Frame>

Beside the tabs:

- **A search box** for the tab you're on. For example, Wells searches API, well, operator, lease and field. Type at least two characters. Several words must all match; put a phrase in quotes to keep it together. A search counts as a filter.
- **Export** downloads what the table is listing, as CSV, or as GeoGraphix, OpenWorks and DRI files. See [Export data](/maps/exports#export-from-the-data-tables).
- **Fill the window** and **×**.

## The toolbar

From left to right:

- **Filters**: narrow the rows. See [Filters & the map](/maps/filters).
- **Filtered set**: once a filter is on, choose whether it applies to **Table + map** or **Table only**. See [The map follows your filters](/maps/filters#the-map-follows-your-filters).
- **Scope**: **In view** shows only rows inside the map's visible area. **Statewide** shows everything the filters match across Texas, and dims the rows that are off the map. Wells starts on Statewide; the other tabs start on In view.
- **Sort**: order rows by any column on screen, or choose **None**. Wells, Fields, Frac, Pads and Rigs can also sort by **Nearest the map centre**.
- **Rows** (P-4 only): **Transfers** or **Filings**.
- **Columns**, at the right: pick one of the **Views** (ready-made column sets, like *Screening*, or *Production* on Leases), or tick columns yourself. Each column shows how filled-in it is. **Row height** switches between **Compact** and **Roomy**.
- **Analyze**, at the far right: charts, type curves and more over these rows. See [Analyze](/maps/charts).

On P-4, a **Transfer window** band above the rows narrows transfers by date: **All**, **Since 2021**, **2011–2020**, **1993–2010** or **Pre-1993**.

### Column headers

- Click a header to sort by it.
- Drag a header to move the column. Drag its edge to resize it; double-click the edge to reset.
- Click the pin on a header to keep that column in view while you scroll sideways.

## The footer

The footer tells you which rows you're seeing (for example *1–50 of 12,345 wells*) and which filters are on. Click the filter summary to edit them. On the right, the **Snapshot** stamp shows how current the data is. Click it to [keep a copy on your device](/maps/offline).

Exports report here too: while a file is written, and when it's done.

## Open a row

Click a row to open its [row card](/maps/row-cards) beside the table. With a card open, <kbd>↑</kbd> / <kbd>↓</kbd> (or <kbd>k</kbd> / <kbd>j</kbd>) step through rows and <kbd>Esc</kbd> closes the card.

While a card is open, the **Tables** handle reads **Detail**. Click it to close the cards.

## Select rows

Tick the checkboxes to select rows. <kbd>Shift</kbd>-click selects a run of rows. The checkbox in the header selects every row in the view (**Select all N wells in this view**).

What you can do with the selection depends on the tab:

| Tab | Actions |
|---|---|
| **Wells** | **Show N on map**, **Export CSV**, **Add to watchlist**, **Copy APIs** |
| **Leases** | **Export CSV**, **Export production**, **Compare production**, **Add to watchlist**, **Add to project scope**, **New project from these**, **Add to deal draft**, **Copy lease keys** |
| **Operators** | **Export CSV**, **Chart production**, **Copy operator numbers** |
| **Fields** | **Export CSV**, **Copy field numbers** |
| **P-4** | **Export CSV**, **Copy filing ids** |
| **Frac** | **Export CSV**, **Copy disclosure ids** |
| **Pads** | **Export CSV**, **Copy detection ids** |
| **Rigs** | **Export CSV**, **Copy site ids**, **Copy APIs** |

**Clear**, at the end of the bar, unticks everything.

<Note>
  **Add to watchlist**, **Add to project scope**, **New project from these** and **Add to deal draft** are greyed out. They belong to Enterprise, which is invite-only for now.
</Note>

{/* enterprise-hold: the Deals row of the Tabs table, the end of the sentence under it, and the Note above.
| **Deals** | Your deals (Enterprise). See [Deals](/enterprise/deals). |

…show their statewide row count, and Deals shows how many deals you have.

<Note>
  Watchlists, projects and deals are part of the [Enterprise workspace](/enterprise/overview). Without it, those buttons are greyed out. **Add to watchlist** and **Add to project scope** also need an [active project](/enterprise/projects).
</Note>
*/}

On Wells, if the map is showing only certain wells and you pan away from all of them, the footer offers **Fly back to them**.
