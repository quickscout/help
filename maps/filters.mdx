---
title: "Filters & the map"
sidebarTitle: "Filters & the map"
description: "Filter the tables, and have the map show only what matches."
---

## Add a filter

<Steps>
  <Step title="Click Filters">
    It's the first button above the table. A badge shows how many filters are on.

    {/* screenshot: data-filters — Filters popover */}
  </Step>
  <Step title="Use a quick filter, or build your own">
    **Quick** pills are one-click filters, with a count of how many rows each would match. For example, on Wells: *horizontal*, *vertical*, *producing*, *has survey*.

    For anything else, click **+ Add filter** and choose a **Column**, an **Operator** (is, between, contains…) and a **Value**. Common values appear as chips you can click.
  </Step>
  <Step title="Edit or remove">
    Every filter stays in the list, where you can change it or remove it with **×**. **Clear all** removes them all.
  </Step>
</Steps>

<Tip>
  On Wells, **Include historic wells** adds old wells that are hidden by default.
  **Inside a My Data layer** keeps only wells inside one of your [uploaded areas](/maps/my-data), by wellbore or by surface hole.
</Tip>

## The map follows your filters

On **Wells** and **Leases**, once a filter is on, **Filtered set** sets where it applies:

- **Table + map**: the map shows only matching wells or leases. This stays on even after you close the tables.
- **Table only**: the map is left alone.

A **Map filtered** badge at the bottom of the map reminds you. Click **Edit filters** on it to change them.

### When the map can't follow

The map can filter up to **100,000** matches at once. If more match, you'll see **too many to filter the map here**. Zoom in, or narrow the filter. When you're zoomed in, the map filters just the area around your view.

To filter leases on the map, the **Lease Footprints** layer must be on. If it's off, the footer offers **Turn on**.

## Show only your selection

Tick rows on the Wells tab and click **Show N on map** to see only those wells. **Back to filters** returns to your filtered set, and **Show all wells** clears it.
